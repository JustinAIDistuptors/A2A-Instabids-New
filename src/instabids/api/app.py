"""Main FastAPI application for InstaBids API."""from fastapi import FastAPIfrom fastapi.middleware.cors import CORSMiddlewarefrom instabids.api.routes.ws_chat import router as ws_routerfrom instabids.api.routes.messages import router as messages_router# Create FastAPI appapp = FastAPI(    title="InstaBids API",    description="API for InstaBids homeowner and contractor services",    version="0.1.0")# Add CORS middlewareapp.add_middleware(    CORSMiddleware,    allow_origins=["*"],  # In production, replace with specific origins    allow_credentials=True,    allow_methods=["*"],    allow_headers=["*"],)# Include routersapp.include_router(ws_router, tags=["websocket"])app.include_router(messages_router, prefix="/api", tags=["messages"])@app.get("/", tags=["health"])async def root():    """Health check endpoint."""    return {"status": "ok", "service": "InstaBids API"}